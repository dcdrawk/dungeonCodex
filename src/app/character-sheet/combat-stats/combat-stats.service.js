(function() {
    'use strict';

    angular
        .module('dc')
        .factory('combatStatsService', combatStatsService);

    // factory.$inject = ['dependencies'];

    /* @ngInject */
    function combatStatsService(dependencies) {
        var service = {
            // function: function
        };

        return service;

        // function function() {
        //
        // }
    }
})();
